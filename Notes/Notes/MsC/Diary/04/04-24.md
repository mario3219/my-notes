Cutout-size has to be 224 for EVA

I've preprocessed like paper and like paper with masking
Now i have to see if embedding now works

I'm rerunning all the preprocessing for CTA and WTA for clean training runs

```
  File "/lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/Image2Count/src/utils/eva_kit/layers.py", line 65, in forward
    m_gene = marker_to_gene[m]
             ~~~~~~~~~~~~~~^^^
KeyError: 'CYTO13'
```
Yes, i need to mask channels

I'm concerned that the patch cut out size being 224 is set in stone, its also not intended for patches to be overlapping

```
feat shape = torch.Size([256, 768])
```

I've started embedding now based on masking or no masking, for cta
Once this is done, im gonna start training for eva
And im starting full runs for deepcell and kronos for cta and wta again

Just the snr filtering tests and cosmx left
snr needs to wait for deepcell and kronos

```
>>> pats = df["Patient_ID"]
>>> pats.unique()
array(['Lung6', 'Lung5_Rep1', 'Lung5_Rep3', 'Lung5_Rep2', 'Lung9_Rep1',
       'Lung9_Rep2', 'Lung12', 'Lung13'], dtype=object)
```

I've queued everything, i can experiment with old runs of deepcell and kronos to test snr filtering