Failed runs
* cta_filt_cta_filt
* cta_filt_subset_wta_filt_subset
* cta_sub_wta_sub
* wta_filt_sub_cta_filt_sub
* wta_filt_wta_filt
* wta_sub_cta_sub
	* deepcell
* wta_wta

Training was successfull, only eval runs crashed due to this error:

```
FileNotFoundError: [Errno 2] No such file or directory: '/out/cta_filt/cta_fil
t/l1h8/0'
```

But wta_sub_cta_sub DEEPCELL stopped on the 10th fold, have to rerun

I'll use cta_filt_cta_filt for debugging.
The embeddings are there, so it must be purely arguments in the eval.sh script.

Now I'm executing all the eval runs, I'm pretty happy with the introduction part of the report now.

Guess I can try and work with EVA, or making a model downloader script

****

Results from evaluation
The performance metrics didn't get generated, so I'm writing the results from corr_area_...mean.csv
in the meantime

DEEPCELL
```
                                         Spearman      MSE      Genes
cta_filt        cta_filt                 0.146                  36
cta_filt_sub    wta_filt_sub             0.158                  46
cta_sub         wta_sub                  0.067                  3
wta             wta
wta_filt        wta_filt                 0.319                  2843
wta_filt_sub    cta_filt_sub            -0.001                  0
wta_sub         cta_sub                 -0.013                  0
```

KRONOS
```
                                         Spearman      MSE      Genes
cta_filt        cta_filt                 0.196                  132
cta_filt_sub    wta_filt_sub             0.191                  35
cta_sub         wta_sub                  0.213                  70
wta             wta
wta_filt        wta_filt                 0.438                  3958
wta_filt_sub    cta_filt_sub             0.208                  106
wta_sub         cta_sub                  0.256                  1507
```

```
WTA              18677
CTA              1812
Subsets          1773
Filtered WTA     4389
filtered CTA     357
Filtered subsets 227
```

Filtered subsets were created by filtering CTA, filtering WTA, and making subsets of each other

Highest performance when trained on WTA
Big boost when filtering WTA
Big drop when making WTA subsets of CTA
Still a drop when making subset of filtered CTA
Better performance when training on WTA and predicting on CTA than vice versa

Does this mean that CTA was noisy?
Could also be that WTA was random split, and CTA was not
Performance dropped when training on filtered CTA, perhaps too many genes got filtered out

![[Pasted image 20260414141859.png]]![[Pasted image 20260414141922.png]]

Note for Markus:
* Add an assert in cellevaluation for path arguments that shouldn't end with /, because some arguments wants to point to a folder, other arguments want to point to the content of a folder

****
EVA

Model needs to be downloaded from hugging_face

I've implemented the model itself into i2c now

"Input MIF images (representing protein expression signals) were clipped and normalized to 0-1 scales using lower and upper bounds calculated via histogram thresholding. H&E images were first scaled to a 0-1 range by dividing pixel values by 255 (for unsigned 8-bit integer format), and then inverted by computing 1 − 𝐼, where 𝐼 represents the scaled pixel values. This inversion generates black-background images that are more consistent with fluorescence images, improving training efficiency."