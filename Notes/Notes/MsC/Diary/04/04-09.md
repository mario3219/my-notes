Anotha day

I will have to start organizing my results, specifically to divide into the 0.6/0.4 split results and the "Sofia" suggestion split results.

Markus suggests making a 15-20% split based on patients for the WTA, I will have to write a script that reads the xlsx file and splits patients, and moves ROI files according to what patients are tied to them.

****
Analysing and working with andata from markus code

Markus extracts true_expression from processed directory, which gets generated when GeoMxDataset is called. He forms graph_...pt files which has these member names
```
x=node_features,
edge_index=edge_index,
edge_attr=edge_attr.to(torch.float32)
pos=torch.from_numpy(coordinates).to(torch.float32)
y=label
cellexpr=cellexpr)
```
I don't want to use node_features, because these are formed from `cells_embed`, im going to see if i can work without these

Problem:
After getting ideal genes from CTA, i only have 182 genes remaining

![[Pasted image 20260409155118.png]]
`182 genes`

![[Pasted image 20260409155543.png]]
`891 genes`

![[Pasted image 20260409155826.png]]
Just an example of WTA

Plans for tomorrow:
* Ask Markus about how to optimize this spread
* Add option for get_labels to exclude genes based on a list outputted by filter_genes
* Make a test/train splitter based on patient IDs
	It should take a ratio as argument, and find corresponding files associated with each patient set to be used for train/test, and then move these files accordingly.

	A tip is that i can use labels.csv to find patient_ids and the corresponding image names.