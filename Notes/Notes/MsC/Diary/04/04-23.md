Best models
* KRONOS
	* 2 linear layers, 4 heads
* Deepcell
	* 1 linear layer, 8 heads

All cta training and eval survived
All wta training failed

Progress:
Preprocessing for cosmx data is now working
I should be able to go into training the models, but there are some problems with the labels data

I haven't tested the SNR filtering yet, and I need to create some threshold csvs and actually do a run

Eva model needs some more work
[Modeling patient tissues at molecular resolution with Eva | bioRxiv](https://www.biorxiv.org/content/10.64898/2025.12.10.693553v1.full#sec-42)

"Input MIF images (representing protein expression signals) were clipped and normalized to 0-1 scales using lower and upper bounds calculated via histogram thresholding. H&E images were first scaled to a 0-1 range by dividing pixel values by 255 (for unsigned 8-bit integer format), and then inverted by computing 1 − 𝐼, where 𝐼 represents the scaled pixel values."

```
ln -s /lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/EVA_funcs/like_paper.py /lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/eva_like_paper/src/utils/eva_kit/image_funcs.py

```
