Reproducibility

Run
```
python ../../../../src/utils/prepare_cosmx.py
python ../../../../src/utils/cosmx_label_and_pos_data.py
python ../../../../src/utils/flip_y_axis.py
```

```
base=/lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/Image2Count
ln -s ${base}/data/raw/cosmx/raw/Lung6/*/*_Z001.TIF ${base}/data/raw/cosmx/test/
ln -s ${base}/data/raw/cosmx/raw/Lung13/*/*_Z001.TIF ${base}/data/raw/cosmx/test/
ln -s ${base}/data/raw/cosmx/raw/Lung5*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
ln -s ${base}/data/raw/cosmx/raw/Lung9*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
ln -s ${base}/data/raw/cosmx/raw/Lung12*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
```

My scripts
```
src/utils/jonte_utils/format_cosmx.py
src/utils/jonte_utils/cosmx_get_cell_ids.py
```

Then my links script
```
base=/lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/Image2Count/data/raw
ln -s ${base}/cosmx/raw/cosmx_label_formatted.csv ${base}/

exp=${base}/cosmx/cosmx_dct

mkdir ${exp}
mkdir ${exp}/masks
mkdir ${exp}/train
mkdir ${exp}/test

ln -s ${base}/cosmx/raw/cosmx_measurements_formatted.csv ${exp}/
ln -s ${base}/cosmx/train/* ${exp}/train/
ln -s ${base}/cosmx/test/* ${exp}/test/
ln -s ${base}/cosmx/masks/* ${exp}/masks/

exp=${base}/cosmx/cosmx_kr

mkdir ${exp}
mkdir ${exp}/masks
mkdir ${exp}/train
mkdir ${exp}/test

ln -s ${base}/cosmx/raw/cosmx_measurements_formatted.csv ${exp}/
ln -s ${base}/cosmx/train/* ${exp}/train/
ln -s ${base}/cosmx/test/* ${exp}/test/
ln -s ${base}/cosmx/masks/* ${exp}/masks/
```

****
New reproduce flow

```
python ../../../../src/utils/prepare_cosmx.py
python ../../../../src/utils/cosmx_label_and_pos_data.py
python ../../../../src/utils/flip_y_axis.py
```

```
base=/lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/Image2Count
ln -s ${base}/data/raw/cosmx/raw/Lung6/*/*_Z001.TIF ${base}/data/raw/cosmx/test/
ln -s ${base}/data/raw/cosmx/raw/Lung13/*/*_Z001.TIF ${base}/data/raw/cosmx/test/
ln -s ${base}/data/raw/cosmx/raw/Lung5*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
ln -s ${base}/data/raw/cosmx/raw/Lung9*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
ln -s ${base}/data/raw/cosmx/raw/Lung12*/*/*_Z001.TIF ${base}/data/raw/cosmx/train/
```

```
src/utils/jonte_utils/format_cosmx.py
```

```
base=/lunarc/nobackup/projects/lu2024-17-35/jonathan/Code/Image2Count/data/raw
ln -s ${base}/cosmx/raw/cosmx_label_formatted.csv ${base}/

exp=${base}/cosmx/cosmx_dct

mkdir ${exp}
mkdir ${exp}/masks
mkdir ${exp}/train
mkdir ${exp}/test

ln -s ${base}/cosmx/raw/cosmx_measurements_formatted.csv ${exp}/
ln -s ${base}/cosmx/train/* ${exp}/train/
ln -s ${base}/cosmx/test/* ${exp}/test/
ln -s ${base}/cosmx/masks/* ${exp}/masks/

exp=${base}/cosmx/cosmx_kr

mkdir ${exp}
mkdir ${exp}/masks
mkdir ${exp}/train
mkdir ${exp}/test

ln -s ${base}/cosmx/raw/cosmx_measurements_formatted.csv ${exp}/
ln -s ${base}/cosmx/train/* ${exp}/train/
ln -s ${base}/cosmx/test/* ${exp}/test/
ln -s ${base}/cosmx/masks/* ${exp}/masks/
```

Using the flipped versions just makes everything wrong??????
