I've been very sick and haven't done any work for two days, let's lock in now...

`SNR-like=gene counts/negative probe counts`
[Analysing Nanostring's GeoMx transcriptomics data using standR, limma and vissE • GeoMXAnalysisWorkflow](https://davislaboratory.github.io/GeoMXAnalysisWorkflow/articles/GeoMXAnalysisWorkflow.html?)

`LOD=mean(neg)+k*std(neg)`
[Spatial transcriptomics - Wikipedia](https://en.wikipedia.org/wiki/Spatial_transcriptomics?)

Do we have negative probes?
Yes, the target is named "Negative Probe" in all CTA panels
and "NegProbe-WTX" in WTA

Sofia
```
[1] "FGF17"    "DKK1"     "CLEC4C"   "MMP7"     "CCL24"    "MS4A4A"   "CCL8"     "COL11A1"  "MLANA"     
[10] "DSC3"     "FGF16"    "CCNB3"    "WNT2B"    "DACH2"    "INHBA"    "IL11"     "CCL23"    "SYCP1"     
[19] "MUC1"     "STC1"     "IL21"     "PLA2G4A"  "CCL20"    "LILRA5"   "FCER1A"   "COL4A5"   "COL4A6"    
[28] "CACNG6"   "CCL28"    "MARCO"    "NOG"      "IL1RAPL2" "GRIA3"    "MAGEB2"
```

[Spatially resolved whole transcriptome profiling in human and mouse tissue using Digital Spatial Profiling - PMC](https://pmc.ncbi.nlm.nih.gov/articles/PMC9712633/)
We found that selecting an LOD threshold of 2 standard deviations above the geometric mean of negative probes reliably achieves a specificity of >95% at all ROI sizes in both panels.
`LOD=mean(neg)+k*std(neg)`

I only have one probe per ROI
So workflow might be
`background=NegProbeCount`
`SNR=x/NegProbeCount`

Or for each ROI
```
detected = 1*(x_count>2*NegProbeCount) <-boolean
detect_rate=sum(detected)/ROIs
```

Might not be good with mean/variance
Because confusing noise with variability across tissues
Highly variable plots already show "SNR" but 

Bugs in reproduce cosmx
```
We want to now create the files necessary to run Image2Count. For that we modify `src/utils/prepare_cosmx.py` in line 5-10:

```py

tif_files = glob.glob(' */*Raw*/*_Z001.TIF')
```
Leading space

And in `prepare_cosmx` misspell, metdata

```
mkdir data/raw/cosmx/train
mdkir data/raw/cosmx/test
```
mdkir

running flip before there are images in train or test?

TODO
* Look at why negative dispersions
	This was due to the log scale, dispersions that fall below 1 get negative exponents
* Make plots of SNR per gene
	I've made these plots, and can now decide on a threshold
* Look at what patients crossover between WTA and CTA
* Try implementing LOQ filtering
* Try filtering for genes Sofia suggested

Plans
* Take my best models, make different thresholds, and make predictions and plot spearman score vs threshold
* Evaluate KRONOS and Deepcell on the cosmx data