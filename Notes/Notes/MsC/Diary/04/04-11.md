For CTA:
```
ScanLabel_dict = {
            "251117_TRIANGLE_4_Barcelona_ROI_2": "251113_TRIANGLE_4_Barcelona_ROI"
            }
SegmentLabel_list = []
```

For WTA:
```
    ScanLabel_dict = {}
    SegmentLabel_list = ['Full ROI']
```

CTA
```
min_mean max_mean min_disp max_disp genes
1        12       1        12       204
1        12       0.5      12       357
```

WTA
```
min_mean max_mean min_disp max_disp genes
1        12       1        12       2218
1        12       0.5      12       4389
```

For CTA, atleast 300 genes
For WTA, atleast 300x10 genes

Best models
* KRONOS
	* 2 linear layers, 4 heads
* Deepcell
	* 1 linear layer, 8 heads

* Train on CTA, check performance (already done)
* Train on WTA
	* Check on WTA
	* Check on CTA
	* Check on CTA subset of WTA
* Train on CTA
	* Check on WTA
	* Check on CTA subset of WTA

Failed training runs:
* Deepcell full WTA
* KRONOS full WTA
