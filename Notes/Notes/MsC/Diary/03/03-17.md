I have a terrible bug in qupath
![[Pasted image 20260317124346.png]]

My script is hallucinating cell positions

The reason is not because of several saveName being identical.

Okay, the reason was in the image_preprocess in python. I was extracting positions using startswith, so for example image_1 will lead to positions from image_10 to be extracted aswell.

586, 106, 476
****
Preprocess output shape
```
patch_markers: (C,H,W)
marker_ids: (C)
```

For the future
```
        if cell_id == 0:
            import matplotlib.pyplot as plt
            plt.imshow(mask_patch)
            save_dir = os.path.join(os.getcwd(),'wrong')
            if not os.path.exists(save_dir):
                os.makedirs(save_dir)
            save_path = os.path.join(save_dir,str(cell_id)+'.png')
            temp = 0
            while os.path.exists(save_path):
                temp += 1
                save_path = os.path.join(save_dir,str(cell_id+temp)+'.png')
            plt.savefig(save_path)
            plt.close()
            continue

```

The model outputs these shapes:
```
patch_features:
patch_marker_features: (384)
patch_token_features:
```
Question is, which one should i use as embeddings. 384 is the embed size of the model. Deepcell also only used a linear vector of embed size.