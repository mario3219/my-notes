Pathway metrics runs now, but only because i set return 0. I will run the final evaluations and start analysis once it is done

These are my current tasks:
* Metrics for models, find optimal hyperparameters
* WTA segmentation
* Apply for EVA access
* Running Markus LaTeX code

EVA link
[Modeling patient tissues at molecular resolution with Eva | bioRxiv](https://www.biorxiv.org/content/10.64898/2025.12.10.693553v1.full#sec-42)

Intended path for metrics_to_latex
`path='figures/metrics/'`
Loop experiment, containing experiments
So
```
figures/metrics/experiment1
figures/metrics/experiment2
...
```

Inner loop, mapping models in experiment
```
figures/metrics/experiment1/model1
figures/metrics/experiment1/model2
...
figures/metrics/experiment2/model1
figures/metrics/experiment2/model2
```

Inside this loop, creates paths to files with this structure
`avg_metrics_....csv`
in folder `figures/metrics/experiment/model/metrics/`
Right now, my csvs has .h5ad in their names... should not be there.
So
```
figures/metrics/experiment/model/metrics/avg_metrics...csv
figures/metrics/experiment/model/metrics/avg_metrics...csv
```

Right now my paths are
```
figures/model/metrics/avg_metrics...csv
```

`path=figures/metrics` can be changed to just figures

So the difference is essentially
```
figures/experiment/model/metrics/avg_metrics...csv
figures/........../model/metrics/avg_metrics...csv
```
Just the experiment, and that h5ad is in the csv name

And afterwards, loops through `avg_metrics` in mean folder
`figures/metrics/experiment/model/metrics/mean/avg_metrics...csv`

All of this fits with my current setup, except for experiment and csvs having h5ad in their names.