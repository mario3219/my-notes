To get perform_metrics, it needs h5ad files, and if those are to be produced, it needs to associate correctly between the true graphs in processed and embedded predictions, so if train and test are different in which files are present, it will crash.

Processed folder contains the true graphs, and will be the same all the time if the train and test contents are the same.

To compare with the old models I trained, I need to reform the old embeddings... but this will be bad because if I form new embeddings with the current split, there will be crossover between the train and test split...