Meeting
filtered spearman fold idx 6, 1807
1 june report hand in

****

metadata for cyto13
```
Global mean: 0.047963022821327465
Global std: 0.03313597842939998
```

Files required:
```
images
cell_masks
	Works with current masks
marker_info_with_metadata
cell_annotations (ground truth)
	image name
	cell_id
		can extract
	x_center
	y_center
	phenotype (ignore)
```

Following the cell phenotyping guide
* CellPhenotyping class gets initialized
	class member for config gets stored
	
	Ignore below...
	These get loaded as class members
	* patch dir
	* patch names
	* marker list from config
	* marker max values from config
	* marker info with metadata
* Method patch_extraction gets called
	* marker_info_with_metadata gets loaded
	* ground truth data gets loaded from config
	* patch size from config
	* patch dir path where patches will be stored gets loaded
	* image paths gets loaded from config
* Inside the method, starts iterating through images
	* Loads an image
	* Loads cell_mask
	* Gets corresponding ground truth data
* And then starts iterating through ground truth data
* Creates boundaries
* Extracts cell patches and mask patches
* Pads if necessary
* Compares cell id in ground truth data and that it exists in mask
* Creates a binary mask where only the cell is set to 1

conclusion: Everything seems fine, just need to adapt to the stuff from config, and the extracting cell id from coordinates and not from ground truth csv. It seems the images are made to be handled as multichannel, not as one channel per image.

Then feature extraction is made by calling method feature_extraction.
* CellPhenotypingDataset is created
* It initializes members from config
* Has inherited methods from dataset interface
* Most importantly, get_item
* The dataset is used as a loader
* Inside feature extraction, passes through model and gets embeddings

****
586, 106, 476
**Analysis**
* It seems to store patches in separate files
	* See if i can store several patches in one file, and each file is an ROI
	* No, just gonna use the directory for each ROI solution
* It loads batches of patches at a time (pytorch dataloader handles it)
* It stores batches as separate files
	* Outer loop, loop through each directory containing each rois patches
	* Load all the patches to collected arrays.
	* Iterate through these as batches
	* Perhaps initialize batch size and data size, and for each batch, load data? More memory efficient