Kronos embed shape
```
patch_features: (B,embed_dim)
patch_markers: (B,C,embed_dim)
patch_token_features: (B,C,C,C,embed_dim)
```

Deepcell embed shape
```
(B,embed_dim)
```

ChatGPT suggested to assign Cyto13 marker_id 13

Maybe its too much to add a csv in data for the user to add their metadata for missing channel, will just be annoying when doing several runs. I will just mask the channel in the image instead.