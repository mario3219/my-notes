Working with KRONOS

In order to run KRONOS, cell_annotations.csv is required.

To form this, run jonte_cell_annotations_run.sh, this requires that the masks in image2count/.../masks do not have `_masks.tif` in their name. I have added bash scripts to rename them at will