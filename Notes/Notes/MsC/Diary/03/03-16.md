I implemented the patch generator for KRONOS, going to run a comparison with the tutorial.

KRONOS bug in image_funcs:
Check how xx and yy is used, because they are reformed as y1 and x1. In original they are reversed when initialized

Comparison with KRONOS default patch extraction and my remake:
![[Pasted image 20260316125956.png]]

Left is image2count, right is KRONOS, so the patch generator works now.

**Move processing in feature_extraction to preprocessing**
Dataloader loads these values, and these are their pairs
```

```