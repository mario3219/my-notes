Started training

There is a bug:
GraphTrain.py line 278: Calls attribute current_fold of GeoMXData
GeoMXData.py line 131: If --num_cfolds <=1, will never initialize the attribute

README is misleading, when embedding it stores results in /out/graph_model/, but when using cellevaluation and the arguments provided, it points to a path containing the pt files, but it doesn't store the paths if the name has .pt in it. Also, if you point to the path before instead, it will not start looping through that file if it has an underscore in the name

do_clusterin in --vis_label_data call