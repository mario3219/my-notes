Family of networks that can handle sequences or time series
Tends to have a gradient problem, where gradients get very small.
For each new input, the memory of earliest samples will be lost, which means that these samples no longer influence the output.