Standardizing algorithms is hard, as different devices are handled differently. All companies has as a goal to make their devices work on their data, but adapting algorithms to each device is rudimentary and impractical.

Some devices has different requirements as well, for example some devices with goal has high sensitivity is more important than high specificity.