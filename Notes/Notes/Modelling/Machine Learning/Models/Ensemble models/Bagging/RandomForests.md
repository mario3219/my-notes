Same as in the overview, random subsets of data are formed. When training each tree however, the amount of inputs used in the splitting criterion are chosen at random at each split. This happens independently for each tree.

So the idea is to inject additional randomness when constructing each tree.