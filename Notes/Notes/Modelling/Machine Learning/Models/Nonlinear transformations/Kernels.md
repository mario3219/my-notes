Input features can be transformed, which results in other optimization results.

Specific transformations that gives specific types of results are called kernels, for example:
* Polynomial kernel
* Squared exponential kernel