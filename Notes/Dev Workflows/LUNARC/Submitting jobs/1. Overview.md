A node is a computer/server in the cluster
You must specify:
1. The number of nodes required by the job
2. The number of tasks per node
3. The number of threads spawned by each task

For pure jobs, one usually only specifies points 1 and 2, while threaded jobs specify 1 and 3
