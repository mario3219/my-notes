**Column and row handling**

```
df.columns
```
This extracts column names

```
df.iloc[:,2]
df.loc[:,'Protein']
```
This extracts column values

It is a good practice to convert to numpy when handling values
`df.iloc[:,1].to_numpy()`