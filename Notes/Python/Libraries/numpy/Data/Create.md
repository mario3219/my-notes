Empty
```
arr = np.empty((1, 1, C), dtype=np.float32)
```

Zeros
```
arr = np.zeros(5)
```

Create zeros array in similar shape as another array
```
marker_embeddings = np.zeros_like(list(marker2embedding.values()), dtype=np.float32)
```

arange
```
np.arange(start,stop,step_size,dtype=)
```

linspace
```
np.linspace(min,max,num_values)
```