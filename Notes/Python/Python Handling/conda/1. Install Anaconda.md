```
cd ~/
curl -O https://repo.anaconda.com/archive/Anaconda3-2025.12-2-Linux-x86_64.sh
```

```
bash ~/Anaconda3-2025.12-2-Linux-x86_64.sh
```

Make sure the following is part of bashrc:

```
source ~/anaconda3/bin/activate
```

Make sure install location has no spaces
Make sure that (base) environment is used at startup