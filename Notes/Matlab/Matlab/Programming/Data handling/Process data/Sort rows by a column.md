```
% Sort rows of A by values in column k by ascending order
A_sorted = sortrows(A, k);

% Sort rows of A by values in column k by descending order
A_sorted = sortrows(A, -k); % negative sign
```